---
const base = import.meta.env.BASE_URL;
---

<section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden">
  <!-- Animated Cloud Background -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute w-96 h-96 bg-white/5 rounded-full blur-3xl -top-20 -left-20 animate-cloud"></div>
    <div class="absolute w-80 h-80 bg-white/5 rounded-full blur-3xl top-1/3 right-0 animate-cloud-reverse"></div>
    <div class="absolute w-72 h-72 bg-white/5 rounded-full blur-3xl bottom-20 left-1/4 animate-cloud"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Main Headline -->
    <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight">
      Driving the Future of
      <span class="block text-accent-orange">India's Mobility</span>
    </h1>

    <!-- Subheadline -->
    <p class="text-lg sm:text-xl md:text-2xl text-text-secondary max-w-3xl mx-auto mb-8 md:mb-12">
      QTCS delivers sustainable electric vehicle solutions that transform transportation
      while protecting our environment for future generations.
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <a
        href={`${base}products`}
        class="w-full sm:w-auto px-8 py-4 bg-accent-orange text-white font-semibold text-lg rounded-full hover:bg-accent-gold transition-colors duration-200 shadow-lg hover:shadow-xl"
      >
        Explore Vehicles
      </a>
      <a
        href={`${base}contact`}
        class="w-full sm:w-auto px-8 py-4 glass text-white font-semibold text-lg rounded-full hover:bg-glass-bg-strong transition-colors duration-200"
      >
        Contact Us
      </a>
    </div>

    <!-- Stats Preview -->
    <div class="mt-16 md:mt-24 grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8">
      <div class="glass-strong rounded-2xl p-6">
        <div class="text-3xl md:text-4xl font-bold text-accent-orange mb-2">7+</div>
        <div class="text-sm md:text-base text-text-muted">Vehicle Models</div>
      </div>
      <div class="glass-strong rounded-2xl p-6">
        <div class="text-3xl md:text-4xl font-bold text-accent-orange mb-2">150+</div>
        <div class="text-sm md:text-base text-text-muted">km Range</div>
      </div>
      <div class="glass-strong rounded-2xl p-6">
        <div class="text-3xl md:text-4xl font-bold text-accent-orange mb-2">Zero</div>
        <div class="text-sm md:text-base text-text-muted">Emissions</div>
      </div>
      <div class="glass-strong rounded-2xl p-6">
        <div class="text-3xl md:text-4xl font-bold text-accent-orange mb-2">Made</div>
        <div class="text-sm md:text-base text-text-muted">in India</div>
      </div>
    </div>
  </div>
</section>
