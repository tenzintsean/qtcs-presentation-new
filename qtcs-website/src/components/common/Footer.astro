---
const currentYear = new Date().getFullYear();
const base = import.meta.env.BASE_URL;

// Get the parent URL (remove /website/ from base to get root)
const rootBase = base.replace('/website/', '/');

const footerLinks = {
  products: [
    { href: `${base}products`, label: 'All Vehicles' },
  ],
  company: [
    { href: `${base}about`, label: 'About Us' },
    { href: `${base}contact`, label: 'Contact' },
  ],
  resources: [
    { href: `${rootBase}`, label: 'Presentation' },
    { href: `${rootBase}style-guide/`, label: 'Style Guide' },
    { href: `${rootBase}downloads/`, label: 'Downloads' },
  ],
};
---

<footer class="bg-dark-overlay mt-auto">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 md:gap-12">
      <!-- Brand -->
      <div class="col-span-1 md:col-span-2 lg:col-span-1">
        <a href={base} class="flex items-center gap-3 mb-4">
          <img src={`${base}qtcslogo.png`} alt="QTCS Logo" class="h-12 w-auto" />
          <span class="text-2xl font-bold text-text-light">QTCS</span>
        </a>
        <p class="text-text-light-muted text-sm leading-relaxed">
          Driving the Future of India's Mobility with sustainable electric vehicle solutions.
        </p>
      </div>

      <!-- Products -->
      <div>
        <h3 class="text-text-light font-semibold mb-4">Products</h3>
        <ul class="space-y-2">
          {footerLinks.products.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-text-light-muted text-sm hover:text-text-light transition-colors duration-200"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Company -->
      <div>
        <h3 class="text-text-light font-semibold mb-4">Company</h3>
        <ul class="space-y-2">
          {footerLinks.company.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-text-light-muted text-sm hover:text-text-light transition-colors duration-200"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Resources -->
      <div>
        <h3 class="text-text-light font-semibold mb-4">Resources</h3>
        <ul class="space-y-2">
          {footerLinks.resources.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-text-light-muted text-sm hover:text-text-light transition-colors duration-200"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="mt-12 pt-8 border-t border-glass-border">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-text-light-muted text-sm">
          &copy; {currentYear} QTCS. All rights reserved.
        </p>
        <p class="text-text-light-muted text-sm">
          Made with care in India
        </p>
      </div>
    </div>
  </div>
</footer>
